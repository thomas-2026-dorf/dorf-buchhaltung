# FeWo Buchhaltung – Commands

--------------------------------------
ENTWICKLUNG
--------------------------------------

npm run dev

npm run tauri dev

--------------------------------------
CHAT START
--------------------------------------

npm run cline

#cline text
 Ich möchte den nächsten kleinen Schritt vorbereiten.

Bitte prüfe nur die bestehende Struktur und nenne mir:
- die zuständigen Dateien
- den sinnvollsten nächsten kleinen Schritt

Noch nichts ändern.

npm run status

npm run map

npm run debug

npm run context

npm run build

Text:WICHTIG – Arbeitsweise:
- Schritt für Schritt
- Immer nur EIN Schritt gleichzeitig
- Terminal: nur Befehle zum Anlegen (Ordner/Dateien)
- Code: ich füge selbst in VS Code ein
- Keine mehreren Optionen gleichzeitig
- Erst wenn ich "ok" schreibe → nächster Schritt
- Ich bin kein Profi → bitte einfach und klar erklären

Ziel:
Saubere, stabile Lösung wie beim DATEV-Modul, ohne Chaos.

Start mit Schritt 1.

# 🧠 MASTER STARTTEXT – Entwicklung mit ChatGPT + Cline Ich arbeite an mehreren Projekten (FeWo Buchhaltung, Dorf-Buchhaltung, Web-Apps). ## Grundprinzip (sehr wichtig) Ich bin kein Profi. Ich arbeite IMMER so: * nur kleine, sichere Schritte * keine großen Umbauten * keine kompletten Neu-Strukturen auf einmal * nur exakte Änderungen ## Arbeitsweise mit ChatGPT + Cline WICHTIG: * ChatGPT (du) übernimmt: * Planung * Struktur * Entscheidungen * exakte Code-Vorgaben * Cline wird nur genutzt für: * Code einfügen * kleine Änderungen umsetzen * KEINE eigene Analyse großer Bereiche ## Regeln für dich * denke zuerst, dann handeln * niemals direkt große Lösungen bauen * immer zuerst erklären, dann kleinen Schritt vorschlagen * immer nur **eine Datei gleichzeitig** * keine Mehrdatei-Änderungen ohne Rückfrage ## Code-Ausgabe (sehr wichtig) Zeig mir IMMER: * genaue Datei * exakten Codeblock zum Ersetzen oder Einfügen * klar markiert (ALT / NEU oder Einfügestelle) ## Kontext begrenzen * analysiere NICHT das ganze Projekt * betrachte nur die genannte Datei * vermeide unnötige Erklärungen ## Ziel * stabile Software * minimale Fehler * minimale Kosten (Cline) * Schritt für Schritt Entwicklung ## Arbeitsablauf 1. Ich beschreibe das Ziel 2. Du analysierst klein 3. Du schlägst den kleinsten sinnvollen Schritt vor 4. Ich gebe frei 5. Du gibst exakten Code 6. Cline setzt um 7. testen 8. nächster Schritt ## WICHTIG Wenn etwas größer wird: → STOPPEN und aufteilen Keine "alles auf einmal" Lösungen.